# 請「複製此檔為 .env」再填寫（與 package.json 同層）。真實金鑰只放 .env，勿改本檔為含密碼版本以免誤提交 Git。
#
# ── TTS：browser（預設）／yating（雅婷，國語+台語）／openai ───────
# VITE_TTS_PROVIDER=browser
# VITE_TTS_PROVIDER=yating
# VITE_TTS_PROVIDER=openai
# 請只設一種雲端；未設或金鑰未就緒時會退回本機 Web Speech。

# ── 雅婷 Yating TTS v2（國語 zh_en_*／台語 tai_*）────────────────
# 文件：https://developer.yating.tw/zh-TW/doc/tts-TTS%20%E8%AA%9E%E9%9F%B3%E5%90%88%E6%88%90v2
# 1) 開發：YATING_API_KEY（勿 VITE_）→ Vite 代理 /api/yating，由伺服器帶 Header key
# 2) 正式：務必後端轉發；demo 可暫用 VITE_YATING_API_KEY（會進 bundle）
#
# VITE_TTS_PROVIDER=yating
#
# 聲線「亦晴 Yiqing」（與引擎無關；引擎仍是 yating）：
# VITE_YATING_VOICE_PROFILE=yiqing
# 可再細調模型 ID（未設時依 profile 用亦晴預設：國語 zh_en_female_2、台語 tai_female_2）
# VITE_YATING_VOICE_MODEL=zh_en_female_2
# VITE_YATING_TAI_VOICE_MODEL=tai_female_2
#
# 動作若有 voicePromptsTai：進訓練頁後由使用者在頂部切國語／台語；台語時雅婷用 tai_*（可選 yatingTaiVoiceModel 覆寫）。
# VITE_YATING_SPEED=0.85
# VITE_YATING_PITCH=1.0
# VITE_YATING_ENERGY=1.0
#
# 開發專用（給 vite.config 代理；請寫在 .env，不要寫在本範例檔）
# YATING_API_KEY=
#
# 正式直連（勿用於產品）
# VITE_YATING_API_KEY=
#
# ── iOS／Android（Capacitor IPA）──────────────────────────────
# 打包前務必設定（Vite 會寫入 bundle，否則 isYatingConfigured 為 false）：
#   VITE_TTS_PROVIDER=yating
#   VITE_YATING_API_KEY=你的金鑰
# 然後：npm run build && npx cap sync ios
# 程式在 App 內改用 CapacitorHttp 呼叫雅婷，以避免 WKWebView fetch 的 CORS 問題。

# ── OpenAI Speech API（可選）──────────────────────────────────
# 文件：https://platform.openai.com/docs/api-reference/audio/createSpeech
# 1) 開發：OPENAI_API_KEY（勿 VITE_）→ Vite 代理 /api/openai，金鑰不進 bundle
# 2) 正式靜態站：需後端轉發，或暫用 VITE_OPENAI_API_KEY（會暴露，僅 demo）
#
# VITE_TTS_PROVIDER=openai
# 可選：模型預設 gpt-4o-mini-tts（支援 instructions）；聲線見官方列表（如 coral、nova）
# VITE_OPENAI_TTS_MODEL=gpt-4o-mini-tts
# VITE_OPENAI_TTS_VOICE=coral
# VITE_OPENAI_TTS_SPEED=0.95
# VITE_OPENAI_TTS_INSTRUCTIONS=Natural conversational Mandarin; calm coach tone, not robotic.
#
# 開發專用（給 vite.config 代理）
# OPENAI_API_KEY=
#
# 正式直連（勿用於產品）
# VITE_OPENAI_API_KEY=
